package src;

/**
 * Command to cancel the checkout process.
 * Clears the cart or resets the checkout state as needed.
 */
public class CancelCheckoutCommand implements Command {
    public void execute() {
        // needs logic
    }
}
